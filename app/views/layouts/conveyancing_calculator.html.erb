<!DOCTYPE html>
<!--[if IE 9]><html lang="en" class="ie ie9 lte9"> <![endif]-->
<html lang="en">
  <head>
    <%= content_for :custom_title do 'Solicitors in London' end %>
    <%= render 'layouts/shared/head' %>
    <%= raw tracking_code('application').head %>
  </head>

  <body class="conveyancing-calculator">
    <%= render 'layouts/tag_manager' %>

    <div class="conveyancing-calculator-header">
      <%= render partial: 'layouts/shared/header',
               locals: {
                 phone: global_site_settings['Phone'],
                 logo: 'logo.png',
                 legal_500: true,
                 solicitors: true
              } %>
    </div>

    <%= yield %>

    <%= render partial: 'layouts/shared/footer',
               locals: {
                 phone: global_site_settings['Phone'],
                 email: global_site_settings['Email']
              } %>

    <%= render 'layouts/shared/javascripts' %>
    <%= raw tracking_code('application').footer %>
  </body>
</html>
