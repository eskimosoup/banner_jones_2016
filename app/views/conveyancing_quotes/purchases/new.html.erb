<%= content_for :content_title do "#{@user.forename.strip}, we just need a bit more information" end %>

<%= render(
  partial: "conveyancing_quotes/purchases/form",
  locals: { conveyancing_quote_purchase: @conveyancing_quote_purchase }
) %>

<p>
  Need a remortgage or transfer of equity?<br />
  Call: <%= render 'shared/calltracks_phone', phone: global_site_settings['Phone'], options: {
     class: 'ga-event',
    data: {
      event_category: 'Offline contact',
      event_action: 'Phone call'
    }
  } %>
</p>
