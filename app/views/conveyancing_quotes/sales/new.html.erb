<%= content_for :content_title { "#{@user.forename}, we just need a bit more information" } %>

<%= render(
  partial: "conveyancing_quotes/sales/form",
  locals: { conveyancing_quote_sale: @conveyancing_quote_sale }
) %>
