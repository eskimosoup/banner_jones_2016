$('.home-team-members-slick').slick('unslick');
$('.home-team-members-slick').html("<%= j render partial: 'team_members/home_team_member', collection: @team_members, as: :team_member if @team_members.present? %>");
if($('.home-team-member').length === 0) {
  $('.home-team-member-profile-information').text('');
  $('.home-team-member-profile-service-name').text('');
}
teamMembersSlick();
